# Server Configuration
NODE_ENV=development
PORT=5000
API_VERSION=v1

# CORS
ALLOWED_ORIGINS=http://localhost:5173,http://localhost:3000

# Neon Database Configuration
# Production connection string with pooler for optimal performance
# Format: postgresql://[user]:[password]@[host]/[database]?sslmode=require&channel_binding=require
DATABASE_URL=postgresql://neondb_owner:npg_cdO6yGexK7wa@ep-nameless-cherry-a1s4bv5a-pooler.ap-southeast-1.aws.neon.tech/DMOR_dev?sslmode=require&channel_binding=require

# Database Pool Settings (optimized for Neon)
DB_POOL_MAX=20
DB_POOL_MIN=2
DB_IDLE_TIMEOUT=30000
DB_CONNECTION_TIMEOUT=10000
DB_SSL_REJECT_UNAUTHORIZED=false

# JWT Secret (for future auth)
JWT_SECRET=your-secret-key-here-change-in-production
JWT_EXPIRES_IN=7d

# Logging
LOG_LEVEL=debug
